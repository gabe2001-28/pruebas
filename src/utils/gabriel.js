export function gabriel(num) {

    if (num > 0) {
        return true; //el numero ingresado es positivo (true)

    } else if (num < 0) {
        return false; //el numero ingresado es negativo (false)

    } else {
        return null; //el numero ingresado es 0 (null)
    }
}